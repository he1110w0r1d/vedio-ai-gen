# 项目定位

本项目是一个 BYOK（Bring Your Own Key，自带 API Key）的 AI 图片与视频资产生成工作台。用户配置自己的模型供应商和 API Key，用统一界面生成、管理、复用图片和视频资产。

# 开发目标

优先把 Stitch 导出的静态 UI 整理成一个可运行、可维护、可扩展的前端应用。第一阶段只做前端与 Mock 数据，不接真实第三方生成 API。

# 技术要求

- 尽量沿用当前项目已有技术栈；
- 如果当前项目是 React/Vite/Tailwind，则继续使用；
- 不要无理由更换框架；
- 不要引入过重依赖；
- 组件要拆分清晰；
- 页面、组件、数据、类型、Mock 服务要分层；
- 保持深色高级创作工作台风格；
- 所有可见 UI 文案使用简体中文；
- 保留 Stitch 已生成的视觉风格，不要大幅改丑。

# 功能优先级

MVP 阶段需要实现：

1. 总览 Dashboard；
2. 项目管理；
3. 供应商与 API Key 配置；
4. 图片生成工作台；
5. 图片资产库；
6. 图片发送到视频生成；
7. 视频生成工作台，包含 T2V、I2V、R2V 三个模式；
8. 任务中心；
9. 视频资产库；
10. 提示词模板库；
11. 设置页面。

# 交互要求

- 所有生成行为先使用 Mock 数据模拟；
- 图片生成时模拟生成任务和结果；
- 视频生成时模拟异步任务进度；
- 任务中心可以看到排队中、生成中、已完成、失败状态；
- 图片资产详情里必须有“用于视频生成”的操作；
- 支持将图片作为 I2V 首帧、I2V 尾帧、R2V 角色参考、R2V 风格参考；
- API Key 页面只做前端模拟，不保存真实密钥到远端；
- API Key 输入框默认脱敏显示；
- 不要在代码中写入真实 API Key。

# 安全要求

- 不得硬编码真实 API Key；
- 不得提交 .env 文件中的敏感信息；
- 不得把用户 API Key 打印到 console；
- Mock 阶段只能使用假数据；
- 真实 API 接入留接口层，不在第一阶段实现。

# 输出要求

每次完成任务后，请说明：

1. 修改了哪些文件；
2. 实现了哪些功能；
3. 如何运行；
4. 哪些功能仍是 Mock；
5. 下一步建议。
